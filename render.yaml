services:
  - type: web
    name: steam-genn-bot
    runtime: python
    plan: free
    region: oregon
    buildCommand: pip install -r requirements.txt
    startCommand: python render_web.py
    healthCheckPath: /health
    envVars:
      - key: DISCORD_TOKEN
        sync: false
